{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
  <!-- Page Heading -->
  <h1 class="my-4">Produit : {{ product.productName }} #{{ product.id }} </h1>
  <hr>
  <div class="col-md-12">
    <p>{{ product.description }}</p>
    <div>
      <span class="badge">Ajouté le {{ product.dateAdd|date('d/m/Y H:i:s')  }}</span>
    </div>

    <hr>
  </div>
</div>
<!-- /.container -->
{% endblock %}
